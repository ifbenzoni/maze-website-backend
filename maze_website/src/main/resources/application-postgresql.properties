database=postgresql

spring.cloud.gcp.sql.database-name=maze-website-db
spring.cloud.gcp.sql.instance-connection-name=psychic-iridium-394316:us-central1:maze-website-db

# Initialize the database since the newly created Cloud SQL database has no tables. The following flag is for Spring Boot 2.5+.
spring.sql.init.mode=always
